import Typography from './Typography';

interface TypographySheet {
  h1: Typography;
  h2: Typography;
  h3: Typography;
  h4: Typography;
  h5: Typography;
  h6: Typography;

  body1: Typography;
  body2: Typography;
  body3: Typography;

  caption1: Typography;
}

export default TypographySheet;
