import Color from './Color';

interface ColorPalette {
  primary: Color;
  secondary: Color;
  background: Color;

  white: Color;
  black: Color;
  red: Color;
  green: Color;
  blue: Color;

  error: Color;
  success: Color;
}

export default ColorPalette;
